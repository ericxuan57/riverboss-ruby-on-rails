- if flash[:notice]
  .alert.alert-success
    = flash[:notice]
    = link_to "x", "#", class: "alert-close"

- if flash[:error] || flash[:alert]
  .alert.alert-error
    = flash[:error] || flash[:alert]
    = link_to "x", "#", class: "alert-close"
